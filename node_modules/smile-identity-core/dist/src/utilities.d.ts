import { OptionsParam } from './shared';
export declare const get_job_status: (partnerId: string, apiKey: string, url: string, userId: string | number, jobId: string | number, { return_history, return_images }: OptionsParam) => Promise<any>;
export declare class Utilities {
    partnerId: string;
    apiKey: string;
    url: string;
    constructor(partner_id: string, api_key: string, sid_server: string | number);
    get_job_status(userId: string | number, jobId: string | number, options?: OptionsParam): Promise<any>;
}
